group=dev.sinopia
mcVersion=26.3-rc-2
# This is the current API version for use in (paper-)plugin.yml files
# During snapshot cycles this should be the anticipated version of the release target
apiVersion=26.3
channel=ALPHA

# Tessera local base: the upstream migration is unfinished, but its historical
# oldPaperCommit update tooling cannot run against a generated local snapshot.
# This flag controls that tooling; it is NOT a declaration of server readiness.
updatingMinecraft=false

org.gradle.configuration-cache=true
org.gradle.caching=true
org.gradle.parallel=true
org.gradle.vfs.watch=false
